/* Ex 1 tarefa 09*/#include <stdio.h>#include <stdlib.h>// Função que retorna o termo da série fibonacci de acordo// com o número digitado pelo usuárioint termoFibnacci(int numero) {    int termo, cont, anterior, proximo, atual;        termo = 0;    cont = 3;        anterior = 1;    atual = 1;    proximo = 0;       switch(numero){        case 0:            termo = 0;        break;                case 1:             termo = 1;        break;                case 2:            termo = 1;        break;                default :                    while(cont <= numero){                proximo = atual + anterior;                anterior = atual;                atual = proximo;                                cont++;            }                        termo = proximo;                                //Fim default   }       return (termo);}int main() {    int posicao;        scanf("%d", &posicao);        printf("%d", termoFibnacci(posicao));       return 0;}