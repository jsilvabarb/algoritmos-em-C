/* Ex 5 tarefa 08 Define valor MINMAX da matriz*/#include <stdio.h>#define MAX 100int main() {    int  linhas, colunas, i, j, menor, linhaMenor, maior,colunaMaior, minmax;        scanf("%d %d", &linhas, &colunas);    int matriz[linhas][colunas];        menor = 100000;    maior = 0;        //Validando o valor máximo de linhas e colunas    if(linhas >= 1 && linhas <= MAX) {        if(colunas >= 1 && colunas <= MAX){               //Laço que vai ler todos os numeros de entrada e atribuí-los            //Na matriz             for(i = 0; i < linhas; i++){                for(j = 0; j < colunas; j++){                    scanf("%d", &matriz[i][j]);                }            }             menor = matriz[0][0];            linhaMenor = 0;                        //Verificando menor elemento da matriz e sua linha            for(i = 0; i < linhas; i++){                                for(j = 0; j < colunas; j++){                     if(matriz[i][j] < menor) {                        menor = matriz[i][j];                        linhaMenor = i;                    }                                    }                                        }                        //Verificando qual o maior el na linha do menor el            for(i = 0; i < linhas; i++){                               if(i == linhaMenor) {                    for(j = 0; j < colunas; j++){                        if(matriz[i][j] > maior){                            maior = matriz[i][j];                            colunaMaior = j;                        }                    }                }                                            }               printf("%d %d", linhaMenor, colunaMaior);        }    }       return 0;}