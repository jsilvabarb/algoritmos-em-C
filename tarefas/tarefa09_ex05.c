/* Ex 5 tarefa 09*/#include <stdio.h>#include <stdlib.h>#define MAX 100// Função que retorna o MINMAX da matrizint minmaxMatriz(int linhas, int colunas, int matriz[linhas][colunas]) {    int i, j, menor, linhaMenor, maior;        menor = 1000000;    maior = 0;        //Verificando menor elemento da matriz e sua linha    for(i = 0; i < linhas; i++){                        for(j = 0; j < colunas; j++){             if(matriz[i][j] < menor) {                menor = matriz[i][j];                linhaMenor = i;            }                            }                        }                //Verificando qual o maior el na linha do menor el    for(i = 0; i < linhas; i++){                       if(i == linhaMenor) {            for(j = 0; j < colunas; j++){                if(matriz[i][j] > maior){                    maior = matriz[i][j];                }            }        }                            }         return (maior);    }int main() {    int  linhas, colunas, i, j;        scanf("%d %d", &linhas, &colunas);    int matriz[linhas][colunas];        //Validando o valor máximo de linhas e colunas    if(linhas >= 1 && linhas <= MAX) {        if(colunas >= 1 && colunas <= MAX){               //Laço que vai ler todos os numeros de entrada e atribuí-los            //Na matriz             for(i = 0; i < linhas; i++){                for(j = 0; j < colunas; j++){                    scanf("%d", &matriz[i][j]);                }            }                                       printf("%d", minmaxMatriz(linhas, colunas, matriz));        }    }       return 0;}