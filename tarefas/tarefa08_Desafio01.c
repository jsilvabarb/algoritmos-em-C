/*Desafio 01 tarefa 08, Define se a matriz é um quadrado mágico*/#include <stdio.h>#define MAX 100int main() {    int tamanho, i, j;    int soma, ref, eMagico;    scanf("%d", &tamanho);        int matriz[tamanho][tamanho];         soma = 0;    eMagico = 1;    //Validando o valor máximo de linhas e colunas    if(tamanho >= 1 && tamanho <= MAX) {               //Laço que vai ler todos os numeros de entrada e atribuí-los        //Na matriz         for(i = 0; i < tamanho; i++){            for(j = 0; j < tamanho; j++){                scanf("%d", &matriz[i][j]);            }        }                 //Somando os elementos da diagonal principal        for(i = 0; i < tamanho; i++){            soma = soma + matriz[i][i];        }                ref = soma;        soma = 0;                //Somando os elementos da diagonal secundária        for(i = 0; i < tamanho; i++){            soma = soma + matriz[i][(tamanho - 1) - i];        }                //Validando a soma da digonal secundária        if(soma == ref){            //Somando os valores de cada linha                       for(i = 0; i < tamanho; i++){                soma = 0;                for(j = 0; j < tamanho; j++){                    soma = soma + matriz[i][j];                                  }                //Validando a soma de cada linha                if(soma != ref) {                    eMagico = -1;                }            }             //Somando os valores de cada coluna                       for(j = 0; j < tamanho; j++){                soma = 0;                for(i = 0; i < tamanho; i++){                    soma = soma + matriz[i][j];                                  }                //Validando a soma de cada coluna                if(soma != ref) {                    eMagico = -1;                }            }         } else {            eMagico = -1;        }                //Informando ao Usuário se a matriz é ou não um quadrado mágico        if(eMagico == 1) {                        printf("%d", ref);        } else {            printf("%d", 0);        }            }       return 0;}