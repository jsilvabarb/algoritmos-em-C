/*Ex 1 tarefa 07Código que lê um número inteiro indicando o tamanho do vetorem seguida lê uma sequência de nmrs inteiros e os guarda no vetorentão verifica qual é o menor número.*/#include <stdio.h>#include <stdlib.h>int main() {    int numero, cont, tamanho, n, maiorNumero;        scanf("%d", &numero);           int conjunto[numero];        //Verificando o tamanho do vetor e guardando em uma var    tamanho = sizeof(conjunto)/ sizeof(int);        cont = 0;    maiorNumero = 0;        //condição indicada no enunciado    if (numero >= 1 && numero <= 100) {        while(cont <= tamanho ) {            scanf("%d", &n);            conjunto[cont] = n;                        if(conjunto[cont] > maiorNumero){                maiorNumero = conjunto[cont];            }                       cont++;        }    }          printf("%d", maiorNumero);        return 0;}