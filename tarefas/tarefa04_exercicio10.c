/*Escreva um programa que leia a quantidade de fitas que ela possui e valor de aluguel que ela compra.-----------------------------------------------------------------------------------------Saída:Faturamento da locadora;Ganho com multas;Quantidade de fitas ao fim do ano.-----------------------------------------------------------------------------------------*/#include <stdio.h>#include <stdlib.h>#include <math.h>int main() {    int qtdFitas, fitasMes, fitasAtraso, fitasPerda, reposicao, qtdFitasFimAno;    float valorAluguel;    double faturamento, multa, ganhoMultas;       scanf("%d %f", &qtdFitas, &valorAluguel);        //Calculando faturamento da locadora    fitasMes = (double) qtdFitas *  1/3;    faturamento = fitasMes * valorAluguel;        //Calculando valor ganho com multas por mês    fitasAtraso = fitasMes * 0.1;    multa = valorAluguel * 0.1;        ganhoMultas = fitasAtraso * multa;        //Calculando qtd fitas final do ano    fitasPerda =  qtdFitas * 0.02;    reposicao =  fitasPerda * 0.1;    qtdFitasFimAno = (qtdFitas - fitasPerda) + reposicao;         printf("%f\n", faturamento);        printf("%f\n", ganhoMultas);            printf("%d", qtdFitasFimAno);    return 0;}