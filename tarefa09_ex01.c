/* Ex 1 tarefa 09*/#include <stdio.h>#include <stdlib.h>// Função que identifica se o número digitado pelo usuário// É ou não primoint ePrimo(int numero) {    int cont, primo;        primo = 1;    cont= 2;       if(numero == 1) {       primo = -1;   } else {        while(cont < numero && primo == 1) {                        if((numero % cont) == 0){                primo = -1;            }                        cont++;        }    }       return (primo);}int main() {    int n;        scanf("%d", &n);        if(ePrimo(n) == 1){        printf("sim");    } else {        printf("nao");    }       return 0;}