/* Ex 4 tarefa 09*/#include <stdio.h>#include <stdlib.h>// Função que retorna a sequência inversaint sequenciaInversa(int numero, int conjunto[numero]) {        int cont, aux, sequenciaInvertida[numero];        cont = numero - 1;    aux = 0;            while (cont >= 0) {                sequenciaInvertida[aux] = conjunto[cont];                 printf("%d ",sequenciaInvertida[aux]);                aux++;        cont--;    }     }int main() {    int n, i;    i = 0;    scanf("%d", &n);        if(n >= 1 && n <= 100){        int conjunto[n];                        while (i <  n){            scanf("%d", &conjunto[i]);            i++;        }                  sequenciaInversa(n, conjunto);                      }       return 0;}