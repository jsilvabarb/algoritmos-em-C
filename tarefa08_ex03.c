/* Ex 1 tarefa 08*/#include <stdio.h>#define MAX 100int main() {    int  linhas, colunas, i, j, maior, linhaM, colunaM;        scanf("%d %d", &linhas, &colunas);        int matriz[linhas][colunas];        maior = 0;        //Validando o valor máximo de linhas e colunas    if(linhas >= 1 && linhas <= MAX) {        if(colunas >= 1 && colunas <= MAX){                       //Laço que vai ler todos os numeros de entrada e atribuí-los            //Na matriz             for(i = 0; i < linhas; i++){                for(j = 0; j < colunas; j++){                    scanf("%d", &matriz[i][j]);                                        //Validando maior elemento                     if(matriz[i][j] > maior) {                        linhaM = i;                        colunaM = j;                        maior = matriz[i][j];                    }                }            }                                 printf("%d %d", linhaM, colunaM);        }    }       return 0;}